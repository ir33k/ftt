#include <assert.h>
#include <stdio.h>
#include "gmi.h"

void
fmt_gmi(char *str, size_t sz, int wrap, int tab)
{
	assert(str);
	assert(sz > 0);
	assert(wrap > 0);
	assert(tab > 0);
	assert(0 && "Not implemented");
}
